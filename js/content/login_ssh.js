jQuery(document).ready(function(){
	jQuery('input[name="user_name"]').val(config.user_admin_ssh);
	jQuery('input[name="user_password"]').val(config.pass_admin_ssh);
	jQuery('button[type="submit"').click();
});